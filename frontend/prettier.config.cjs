/** @type {import('prettier').Config} */
module.exports = {
  // Предпочитаем одинарные кавычки для JS/TS/JSX.
  singleQuote: true,
  // Ставим запятые во всех возможных местах (включая последнюю в списках) для
  // чище диффов и упрощённого добавления новых элементов.
  trailingComma: 'all',
  // Максимальная длина строки до переноса — помогает держать код читаемым.
  printWidth: 100,
  // Принудительно добавляем точку с запятой в конце каждой инструкции.
  semi: true,
  // Размер отступа в пробелах; используется и для форматирования, и для автофикс.
  tabWidth: 2,
};



